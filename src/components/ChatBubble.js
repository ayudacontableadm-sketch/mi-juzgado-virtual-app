import { StyleSheet, Text, View } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { colors, radii, spacing, typography } from '../theme/theme';
export default function ChatBubble({ message }) { return <View style={[styles.row, message.own && styles.ownRow]}>{!message.own && <View style={styles.avatar}><Ionicons name={message.role === 'Juez' ? 'hammer-outline' : 'person-outline'} size={16} color={colors.white} /></View>}<View style={[styles.bubble, message.own && styles.ownBubble]}><View style={styles.header}><Text style={[styles.role, message.own && styles.ownText]}>{message.role}</Text><Text style={[styles.time, message.own && styles.ownMuted]}>{message.time}</Text></View><Text style={[styles.text, message.own && styles.ownText]}>{message.text}</Text></View></View>; }
const styles = StyleSheet.create({ row: { flexDirection: 'row', alignItems: 'flex-start', gap: spacing.sm, marginBottom: spacing.md }, ownRow: { justifyContent: 'flex-end' }, avatar: { width: 32, height: 32, borderRadius: 16, backgroundColor: colors.primary, alignItems: 'center', justifyContent: 'center' }, bubble: { flexShrink: 1, maxWidth: '84%', backgroundColor: colors.surface, borderRadius: radii.md, padding: spacing.md, borderWidth: 1, borderColor: colors.border }, ownBubble: { backgroundColor: colors.primary, borderColor: colors.primary }, header: { flexDirection: 'row', justifyContent: 'space-between', gap: spacing.md, marginBottom: spacing.xs }, role: { ...typography.tiny, color: colors.primary, textTransform: 'uppercase', fontWeight: '800' }, time: { ...typography.tiny, color: colors.muted }, text: { ...typography.small, color: colors.text }, ownText: { color: colors.white }, ownMuted: { color: '#B8DAE1' } });
