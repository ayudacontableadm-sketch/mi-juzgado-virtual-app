import { StyleSheet, Text, View } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { colors, spacing, typography } from '../theme/theme';
export default function Stepper({ steps, current = 0, compact = false }) { return <View style={styles.row}>{steps.map((step, index) => <View key={step} style={styles.step}><View style={styles.lineRow}>{index > 0 && <View style={[styles.line, index <= current && styles.activeLine]} />}<View style={[styles.circle, index <= current && styles.activeCircle]}><Ionicons name={index < current ? 'checkmark' : 'document-text-outline'} size={compact ? 11 : 13} color={index <= current ? colors.white : colors.muted} /></View>{index < steps.length - 1 && <View style={[styles.line, index < current && styles.activeLine]} />}</View><Text numberOfLines={2} style={[styles.label, index <= current && styles.activeLabel]}>{step}</Text></View>)}</View>; }
const styles = StyleSheet.create({ row: { flexDirection: 'row' }, step: { flex: 1, alignItems: 'center' }, lineRow: { flexDirection: 'row', alignItems: 'center', width: '100%' }, line: { flex: 1, height: 2, backgroundColor: colors.border }, activeLine: { backgroundColor: colors.primary }, circle: { width: 25, height: 25, borderRadius: 13, alignItems: 'center', justifyContent: 'center', backgroundColor: '#E8ECEE' }, activeCircle: { backgroundColor: colors.primary }, label: { ...typography.tiny, marginTop: spacing.xs, textAlign: 'center', color: colors.muted }, activeLabel: { color: colors.primary, fontWeight: '700' } });
