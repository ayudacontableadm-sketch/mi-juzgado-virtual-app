import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { Ionicons } from '@expo/vector-icons'; import AppButton from '../components/AppButton'; import AppCard from '../components/AppCard'; import ScreenHeader from '../components/ScreenHeader'; import Stepper from '../components/Stepper'; import { exampleCase } from '../mocks/mockData'; import { colors, spacing, typography } from '../theme/theme';
const info = [['Materia', exampleCase.subject, 'book-outline'], ['Tipo de juicio', exampleCase.trialType, 'folder-open-outline'], ['Pretensión', exampleCase.claim, 'flash-outline'], ['Cuantía', exampleCase.amount, 'cash-outline'], ['Etapa procesal', exampleCase.proceduralStage, 'calendar-outline']];
export default function CaseInfoScreen({ navigation }) { return <View style={styles.screen}><ScreenHeader navigation={navigation} title={exampleCase.title} right={<Ionicons name="ellipsis-horizontal" size={20} color={colors.ink} />} /><ScrollView contentContainerStyle={styles.content}><Stepper steps={['Información', 'Audiencia', 'Desarrollo', 'Resultado']} current={0} /><AppCard style={styles.card}><Text style={styles.cardTitle}>Información del caso</Text><Text style={styles.description}>{exampleCase.description}</Text>{info.map(([label, value, icon]) => <View key={label} style={styles.detail}><Ionicons name={icon} size={17} color={colors.primary} /><Text style={styles.detailLabel}>{label}</Text><Text style={styles.detailValue}>{value}</Text></View>)}</AppCard><AppCard style={styles.card}><Text style={styles.cardTitle}>Roles en el juicio</Text>{exampleCase.roles.map(([role, actor]) => <View key={role} style={styles.detail}><Ionicons name="person-outline" size={17} color={colors.primary} /><Text style={styles.detailLabel}>{role}</Text><Text style={styles.detailValue}>{actor}</Text></View>)}</AppCard><AppButton title="Iniciar audiencia" onPress={() => navigation.navigate('HearingRoom')} style={styles.button} /></ScrollView></View>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: colors.white }, content: { padding: spacing.lg, gap: spacing.lg, paddingBottom: spacing.xxl }, card: { gap: spacing.md }, cardTitle: { ...typography.h3, color: colors.ink }, description: { ...typography.body, color: colors.text, paddingBottom: spacing.sm, borderBottomWidth: 1, borderBottomColor: colors.border }, detail: { flexDirection: 'row', alignItems: 'center', gap: spacing.sm, borderBottomWidth: 1, borderBottomColor: colors.border, paddingBottom: spacing.sm }, detailLabel: { ...typography.small, color: colors.text, flex: 1 }, detailValue: { ...typography.small, color: colors.ink, flex: 1 }, button: { marginTop: spacing.sm } });
