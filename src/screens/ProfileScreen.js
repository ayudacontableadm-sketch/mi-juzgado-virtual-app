import { Alert, StyleSheet, Text, View } from 'react-native'; import { Ionicons } from '@expo/vector-icons'; import AppButton from '../components/AppButton'; import ScreenContainer from '../components/ScreenContainer'; import AppCard from '../components/AppCard'; import BottomTabs from '../components/BottomTabs'; import { colors, spacing, typography } from '../theme/theme'; import { getCurrentUser, requestDeviceChange } from '../services/authService';
export default function ProfileScreen({ navigation }) { async function requestChange() { const user = getCurrentUser(); if (!user) return Alert.alert('Demo navegable', 'Inicia sesión con el flujo SMS de prueba para registrar una solicitud local.'); try { await requestDeviceChange(user); Alert.alert('Solicitud registrada', 'La solicitud de demostración se guardó localmente en esta sesión.'); } catch { Alert.alert('No se pudo enviar', 'Revisa la conexión y vuelve a intentarlo.'); } } return <ScreenContainer edges={['top', 'right', 'left']} style={styles.screen}><View style={styles.content}><Text style={styles.title}>Perfil</Text><AppCard style={styles.profile}><View style={styles.avatar}><Ionicons name="person-outline" size={28} color={colors.white} /></View><View><Text style={styles.name}>Abogado</Text><Text style={styles.meta}>Cuenta de práctica</Text></View></AppCard><AppCard style={styles.device}><Text style={styles.section}>Dispositivo vinculado</Text><Text style={styles.copy}>Tu cuenta solo puede utilizarse en un dispositivo a la vez. Si cambiaste de equipo, envía una solicitud a soporte.</Text><AppButton variant="secondary" title="Solicitar cambio de dispositivo" onPress={requestChange} /></AppCard></View><BottomTabs navigation={navigation} active="Perfil" /></ScreenContainer>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: colors.white }, content: { flex: 1, padding: spacing.lg, paddingTop: spacing.xl, gap: spacing.lg }, title: { ...typography.h1, color: colors.ink }, profile: { flexDirection: 'row', alignItems: 'center', gap: spacing.md }, avatar: { width: 54, height: 54, borderRadius: 27, backgroundColor: colors.primary, alignItems: 'center', justifyContent: 'center' }, name: { ...typography.h2, color: colors.ink }, meta: { ...typography.small, color: colors.muted }, device: { gap: spacing.md }, section: { ...typography.h3, color: colors.ink }, copy: { ...typography.body, color: colors.text } });
